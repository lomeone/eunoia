name: Publish security module to Github Package

on:
  push:
    branches: [main]
    paths:
      - 'security/**'

jobs:
  publish:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/package-publish.yaml
    with:
      module: 'security'
